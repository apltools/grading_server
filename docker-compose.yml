version: '3'
services:
  app:
    build:
      context: ./app
    ports:
      - "5000:80"
    tty: true
    links:
      - "scheduler:scheduler"
  scheduler:
    build:
      context: ./scheduler
    ports:
      - "4000:80"
    tty: true
    volumes:
      - "/var/run/docker.sock:/var/run/docker.sock"
  redis:
    image: "redis:alpine"
