version: '3'
services:
  scheduler:
    build:
      context: ./app
    ports:
      - "5000:80"
    tty: true
    volumes:
      - "./app:/app"
      - "/var/run/docker.sock:/var/run/docker.sock"
  redis:
    image: "redis:alpine"
