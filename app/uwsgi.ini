[uwsgi]
master = true
https = 0.0.0.0:5000,certs/pub.pem,certs/priv.pem
wsgi-file = app.py
callable = app
processes = 4
threads = 2
add-header = Access-Control-Allow-Origin: *
; logto = /tmp/scheduler.log
loglevel = debug
catch-exceptions = true